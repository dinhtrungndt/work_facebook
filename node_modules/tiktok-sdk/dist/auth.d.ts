declare type authResponse = {
    data: {
        open_id: string;
        scope: string;
        access_token: string;
        expires_in: number;
        refresh_token: string;
        refresh_expires_in: number;
    };
};
declare type authErrorResponse = {
    data: {
        captcha: string;
        desc_url: string;
        description: string;
        error_code: number;
    };
    message: string;
};
export declare class Auth {
    private _clientKey?;
    private _clientSecret?;
    constructor(clientKey?: string, clientSecret?: string);
    getAccessToken(authCode: string): Promise<authResponse | authErrorResponse>;
    refreshAccessToken(refreshToken: string): Promise<authResponse | authErrorResponse>;
    revokeAccessToken(openId: string, accessToken: string): Promise<any>;
}
export {};
