declare type userData = {
    open_id: string;
    union_id: string;
    avatar: string;
    avatar_larger: string;
    display_name: string;
    error_code: number;
    description: string;
};
export declare class User {
    getUserData(openId: string, accessToken: string): Promise<userData>;
}
export {};
