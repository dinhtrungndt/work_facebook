declare enum fieldEnum {
    embed_html = "embed_html",
    embed_link = "embed_link",
    like_count = "like_count",
    comment_count = "comment_count",
    share_count = "share_count",
    view_count = "view_count",
    title = "title"
}
declare type postVideoListRequest = {
    openId: string;
    accessToken: string;
    cursor?: number;
    maxCount?: number;
    fields?: fieldEnum[];
};
declare type VideoListVideoInfo = {
    create_time: number;
    cover_image_url: string;
    share_url: string;
    video_description: string;
    duration: number;
    height: number;
    width: number;
    id: string;
    title?: string;
    embed_html?: string;
    embed_link?: string;
    like_count?: number;
    comment_count?: number;
    share_count?: number;
    view_count?: number;
};
declare type videoListResponse = {
    data?: {
        cursor: number;
        has_more: boolean;
        videos: VideoListVideoInfo[];
    };
    error?: {
        code: number;
        message: string;
        log_id: string;
    };
};
export declare class VideoList {
    getVideoListWithMetadata({ openId, accessToken, cursor, maxCount, fields }: postVideoListRequest): Promise<videoListResponse>;
    getVideoList({ openId, accessToken, cursor, maxCount }: postVideoListRequest): Promise<videoListResponse>;
}
export {};
